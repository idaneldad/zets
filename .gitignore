/target
/Cargo.lock.old
**/*.rs.bk
*.pdb

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Runtime directories (not source)
/cache/
/overlay.enc
/packs/downloaded/

# Large extracted data dumps — regenerate from scripts/ when needed.
# Source dumps are on the Oracle server at /home/dinio/cortex-v7/ and
# /home/dinio/lev-knowledge/; scripts in scripts/ produce these outputs.
/data/hebrew/wiki_full.tsv
/data/hebrew/wiktionary/
/data/multilingual/

# Note: /data/hebrew/core_vocabulary.tsv, prefixes.tsv, suffixes.tsv ARE tracked
#       (small, part of build.rs compilation)
data/packs/
research/.venv/

# New versioned data subdirs — SMALL, tracked:
# data/baseline/ (< 100KB per snapshot)
# data/benchmarks/ (JSONL)
# data/corpora/ (source texts)
# data/installer/ (encrypted blobs, small)
# These are explicit exceptions to the large-data rules above.

# Wikipedia-scale snapshots — regeneratable from data/wikipedia_dumps/ via stream-ingest
data/baseline/wiki_*.atoms
data/baseline/wiki_*.manifest.json

# stream-ingest logs (regenerated per run)
data/log/
py_testers/__pycache__/

# Autonomous learning runtime data — regenerated by night_learner
data/autonomous/
data/autonomous_cache/
mcp/autonomous/status.json
mcp/autonomous/multilang_status.json
data/autonomous/hash_registry_sidecar.json

# Wikipedia dumps — 17GB, regenerate from scripts/fetch_wikipedia.sh
data/wikipedia_dumps/

data/baseline/v4_pilot.zv4
data/baseline/_archive/
data/clients_backup_*/
data/baseline/v4_fixed.zv4
data/baseline/v4_minimal.zv4
data/baseline/*.zv4
